export interface MenuDestination {
  name: string;
  value: boolean;
}
export interface DataDestination {
  imagePath: string;
  title: string;
  description: string;
  distance: string;
  time: string;
}
